<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="mytitlestyle" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:labelTextSize">5sp</item>
        <item name="android:keyTextSize">5sp</item>
        <item name="actionBarSize">50dp</item>
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>
    <style name="MyActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:background">@color/myactionbarcolor</item>
        <item name="background">@color/myactionbarcolor</item>
    </style>
</resources>
